---
# Custom override of jekyll-feed's generated feed so that posts marked
# `draft: true` are excluded from RSS/Atom (the plugin has no per-post filter).
# jekyll-feed sees this source file and skips generating its own.
sitemap: false
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom"{% if site.lang %} xml:lang="{{ site.lang }}"{% endif %}>
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ page.url | absolute_url }}" rel="self" type="application/atom+xml" />
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html"{% if site.lang %} hreflang="{{ site.lang }}"{% endif %} />
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ page.url | absolute_url | xml_escape }}</id>
  {% assign title = site.title | default: site.name %}
  {% if title %}<title type="html">{{ title | smartify | xml_escape }}</title>{% endif %}
  {% if site.description %}<subtitle>{{ site.description | xml_escape }}</subtitle>{% endif %}
  {% if site.author %}
  <author>
    <name>{{ site.author.name | default: site.author | xml_escape }}</name>
    {% if site.author.email %}<email>{{ site.author.email | xml_escape }}</email>{% endif %}
  </author>
  {% endif %}
  {% comment %} Drafts are unlisted — keep them out of the feed too. {% endcomment %}
  {% assign posts = site.posts | where_exp: "post", "post.draft != true" %}
  {% for post in posts limit: 20 %}
  <entry{% if post.lang %} xml:lang="{{ post.lang }}"{% endif %}>
    <title type="html">{{ post.title | smartify | strip_html | normalize_whitespace | xml_escape }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}" />
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
    <id>{{ post.id | absolute_url | xml_escape }}</id>
    <content type="html" xml:base="{{ post.url | absolute_url | xml_escape }}">{{ post.content | strip | xml_escape }}</content>
    {% assign post_author = post.author | default: site.author %}
    {% if post_author %}
    <author>
      <name>{{ post_author.name | default: post_author | xml_escape }}</name>
    </author>
    {% endif %}
    {% if post.summary %}<summary type="html">{{ post.summary | strip_html | normalize_whitespace | xml_escape }}</summary>{% endif %}
    {% for tag in post.tags %}<category term="{{ tag | xml_escape }}" />{% endfor %}
  </entry>
  {% endfor %}
</feed>
